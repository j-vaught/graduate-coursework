\documentclass[10pt,landscape]{article}
\usepackage[landscape,margin=0.5in]{geometry}
\usepackage{multicol}
\usepackage{amsmath,amssymb,amsfonts}
\usepackage{enumitem}
\usepackage{titlesec}
\usepackage{fancyhdr}
\usepackage{xcolor}

\pagestyle{empty}
\setlength{\parindent}{0pt}
\setlength{\parskip}{2pt}

\titleformat{\section}{\bfseries\normalsize}{}{0em}{}[\titlerule]
\titleformat{\subsection}{\bfseries\small}{}{0em}{}
\titlespacing{\section}{0pt}{4pt}{2pt}
\titlespacing{\subsection}{0pt}{3pt}{1pt}

\begin{document}
\raggedcolumns
\begin{multicols*}{3}

\section{Image Operations}
\small

\subsection{Intensity Transformations ($s = T(r)$)}
\textbf{Negative} ($s = L{-}1{-}r$):
Inverts all intensities. Dark becomes bright, bright becomes dark.

\textbf{Log Transform} ($s = c\log(1+r)$):
Compresses high intensities, expands low ones. Reveals detail in dark regions.

\textbf{Power-Law / Gamma} ($s = cr^\gamma$):
$\gamma < 1$: brightens dark regions.
$\gamma > 1$: darkens image, suppresses low intensities.

\textbf{Contrast Stretching}:
Maps a narrow intensity range to the full $[0, L{-}1]$ range. Increases overall contrast.

\textbf{Intensity-Level Slicing}:
Highlights a specific intensity band while suppressing or preserving all others.

\textbf{Bit-Plane Slicing}:
Decomposes into 8 binary planes. Higher planes carry structure; lower planes carry fine detail/noise.

\textbf{Thresholding} ($s = 0$ or $L{-}1$):
Produces binary image. Separates foreground from background.

\subsection{Histogram Operations}
\textbf{Histogram Equalization}:
Flattens histogram to use full intensity range. Increases global contrast.

\textbf{Histogram Matching}:
Reshapes histogram to match a specified target distribution.

\textbf{Local Histogram Processing}:
Applies equalization in sliding neighborhoods. Reveals local detail that global methods miss.

\subsection{Arithmetic Operations}
\textbf{Addition / Averaging}:
$\bar{g}(x,y) = \frac{1}{K}\sum g_i(x,y)$. Reduces noise variance by $1/K$. Images must be registered.

\textbf{Subtraction}:
Highlights differences between two images. Removes static background.

\textbf{Multiplication / Division}:
Mask with ROI to isolate regions. Division corrects uneven illumination/shading.

\subsection{Spatial Filtering}
\textbf{Smoothing (Box / Gaussian)}:
Averages neighborhood pixels. Reduces noise but blurs edges.

\textbf{Median Filter} (nonlinear):
Replaces pixel with neighborhood median. Removes salt-and-pepper noise while preserving edges.

\textbf{Sharpening (Laplacian, Unsharp Mask)}:
Enhances edges and fine detail. Makes transitions crisper.

\subsection{Geometric Operations}
\textbf{Scaling / Resizing}:
Requires interpolation---NN is blocky, bilinear is smooth, bicubic preserves detail best.

\textbf{Rotation / Translation}:
Repositions image. Requires resampling via interpolation at non-integer coordinates.

\subsection{Logic Operations (Binary)}
\textbf{NOT}: swaps foreground/background.
\textbf{AND}: intersection of two masks.
\textbf{OR}: union of two masks.
\textbf{XOR}: pixels in one mask but not both.

\end{multicols*}
\end{document}

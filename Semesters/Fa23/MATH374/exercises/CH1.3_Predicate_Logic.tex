\documentclass{article}
\usepackage[margin=0.75in]{geometry}
\usepackage{amssymb, amsmath}

\newcommand{\answerline}{\newline\newline\noindent\rule{0.95\textwidth}{0.4pt}}

\begin{document}

\section*{Chapter 1.3}

\noindent
2. What is the truth values of each of the following wffs in the interpretation where the domain consists of integers?
\begin{itemize}
    \item[a.] $(\forall x)(\forall y)(x < y \vee y < x)$
    \answerline
    \answerline
    \answerline
    \item[b.] $(\forall x)[x < 0 \rightarrow (\exists y)(y > 0 \wedge x + y = 0)]$
        \answerline
    \answerline
    \answerline
    \item[c.] $(\exists x)(\exists y)(x^2 = y)$
    \answerline
    \answerline
    \answerline
    \item[d.] $(\forall x)(x^2 > 0)$
    \answerline
    \answerline
    \answerline
\end{itemize}

\newpage
\noindent
8. Which of the following are equivalent to the statement "Cats are smarter than dogs."
\begin{itemize}
    \item[a.] Some cats are smarter than some dogs.
    \answerline
    \answerline
    \item[b.] There is a cat that is smarter than all dogs.
    \answerline
    \answerline
    \item[c.] All cats are smarter than all dogs.
    \answerline
    \answerline
    \item[d.] Only cats are smarter than dogs.
    \answerline
    \answerline
    \item[e.] All cats are smarter than any dog.
    \answerline
    \answerline
\end{itemize}

\newpage
\noindent
9. Using the predicate symbols shown and appropriate quantifiers, write each English language statement as a predicate wff. (The domain is the whole world.)

D(x) is "x is a day."

S(x) is "x is sunny."

R(x) is "x is rainy."

M is "Monday."

T is "Tuesday."

\begin{itemize}
    \item[d.] Some days are sunny and rainy.
    \answerline
    \answerline
    \answerline
    \item[e.] No day is both sunny and rainy.
    \answerline
    \answerline
    \item[f.] It is always a sunny day only if it is a rainy day.
    \answerline
    \answerline
    \answerline
    \item[g.] No day is sunny.
    \answerline
    \answerline
    \item[h.] Monday was sunny, therefore every day will be sunny.
    \answerline
    \answerline
    \answerline
    \item[i.] It rained both Monday and Tuesday.
    \answerline
    \answerline
    \answerline
    \item[j.] If some day is rainy, then every day will be sunny.
    \answerline
    \answerline
    \answerline
\end{itemize}

\noindent
18. Give English language translations of the following wffs if

\( L(x, y) \) is "x loves y."

\( H(x) \) is "x is handsome."

\( M(x) \) is "x is a man."

\( P(x) \) is "x is pretty."

\( j \) is "John."

\( k \) is "Kathy."

\( W(x) \) is "x is a woman."

\begin{itemize}
    \item[c.] \( (\forall x)[W(x) \rightarrow (\forall y)[L(x,y) \rightarrow M(y) \land H(y)]] \)
    \answerline
    \answerline
    \answerline
    \item[d.] \( (\exists x)[M(x) \land H(x) \land L(x, k)] \)
    \answerline
    \answerline
    \answerline
    \item[e.] \( (\exists x)[W(x) \land P(x) \land (\forall y)[L(x, y) \rightarrow H(y) \land M(y)]] \)
    \answerline
    \answerline
    \answerline
    \item[f.] \( (\forall x)[W(x) \land P(x) \rightarrow L(j, x)] \)
    \answerline
    \answerline
    \answerline
\end{itemize}

\newpage
\noindent
25. Give interpretations to prove that each of the following wffs is not valid:
\begin{itemize}
    \item[b.] \( (\forall x)(\exists y)P(x, y) \rightarrow (\exists x)(\forall y)P(x, y) \)
    \answerline
    \answerline
    \answerline
    \item[c.] \( (\forall x)[P(x) \rightarrow Q(x)] \rightarrow [(\exists x)P(x) \rightarrow (\forall x)Q(x)] \)
    \answerline
    \answerline
    \answerline
    \item[d.] \( (\forall x)[A(x)] \leftrightarrow [(\forall x)A(x)]' \)
    \answerline
    \answerline
    \answerline
\end{itemize}

\end{document}
